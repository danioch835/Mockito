pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                dir("${env.WORKSPACE}") {
                    sh 'cd SimpleMockito'
                    sh './gradlew test'
                    junit '**/build/**/*.xml'
                }
            }
        }
    }
}
