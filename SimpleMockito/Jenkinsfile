pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                dir("${env.WORKSPACE}") {
                    sh 'cd SimpleMockito'
                    sh 'pwd'
                    sh 'ls'
                    sh './gradlew test'
                    junit '**/build/**/*.xml'
                }
            }
        }
    }
}
