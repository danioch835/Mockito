pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                sh 'cd SimpleMockito'
                gradlew test
                junit '**/build/**/*.xml'
            }
        }
    }
}
